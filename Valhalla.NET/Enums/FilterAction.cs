namespace FPH.ValhallaNET.Enums
{
    // An enum to represent the action to apply to the filtered route
    public enum FilterAction
    {
        Exclude, // Exclude the edges that match the attributes from the route
        Include // Include only the edges that match the attributes in the route
    }
}
